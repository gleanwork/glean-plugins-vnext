{
  "mcpServers": {
    "glean-local": {
      "command": "bash",
      "args": ["./start.sh"],
      "cwd": ".",
      "env": {
        "ENABLE_HITL": "true",
        "HITL_TIMEOUT_MS": "300000"
      }
    }
  }
}
