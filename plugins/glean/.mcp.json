{
  "mcpServers": {
    "glean": {
      "command": "bash",
      "args": ["${CLAUDE_PLUGIN_ROOT}/start.sh"],
      "env": {
        "ENABLE_HITL": "true",
        "HITL_TIMEOUT_MS": "300000",
        "HITL_AUTO_APPROVE": "false"
      }
    }
  }
}
