{
  "mcpServers": {
    "glean": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/start.mjs"],
      "env": {
        "ENABLE_HITL": "true",
        "HITL_TIMEOUT_MS": "300000"
      }
    }
  }
}
